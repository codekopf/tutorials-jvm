package com.codepills.varargs;

import java.util.ArrayList;
import java.util.List;

import com.codepills.BankAccount;

// Varargs is in Java since Java 5. Yet it is not very spreaded feature and you shall meet it very sparsly (really not so often).

public class BankAccountService {

    public BankAccount[] filterBankAccounts(BankAccountFilter... bankAccountFilters) {
        String.format()
        final List<BankAccount> filteredBankAccounts = new ArrayList<>()

        for(final BankAccount bankAccount : bankAccountFilters) {
            switch ()
        }

        return filteredBankAccounts;
    }
}
